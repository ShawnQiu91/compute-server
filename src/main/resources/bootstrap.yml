eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/

spring:
  cloud:
    # 配置读取信息
    config:
      name: compute-server
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: compute-config-center